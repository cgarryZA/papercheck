# papercheck

A reproducible audit harness for mathematical LaTeX papers.

`papercheck` provides a deterministic Python core exposed via a Typer CLI and an
MCP (FastMCP) server for auditing mathematical LaTeX papers.

## Install (development)

```bash
python -m pip install -e ".[dev]"
```

## Usage

```bash
papercheck --help
```
