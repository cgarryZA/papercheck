You are creating an equation index for a mathematical paper.

Create Paper_Audit/06_equation_index.md.

If a papercheck MCP server is available, submit this data via the corresponding MCP tool (e.g. submit_issue, adjudicate_issue, save_inventory_record); the Markdown file is a fallback view.

For every numbered or central displayed equation, record:
- label, if any;
- location;
- exact equation or compressed representation;
- objects appearing in it;
- assumptions needed for it to make sense;
- later equations/theorems that use it;
- whether it is definition, identity, estimate, residual, scheme, bound, or experiment metric.

Do not audit yet except to flag undefined symbols as notes.
