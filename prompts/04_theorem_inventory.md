You are creating a theorem and proof dependency inventory for a mathematical paper.

Do not audit correctness yet except to flag obvious missing information as inventory notes.

Create Paper_Audit/04_theorem_inventory.md.

If a papercheck MCP server is available, submit this data via the corresponding MCP tool (e.g. submit_issue, adjudicate_issue, save_inventory_record); the Markdown file is a fallback view.

For every theorem, lemma, proposition, corollary, definition, assumption, and important remark, record:
- exact statement or faithful compressed statement;
- location;
- assumptions;
- constants and claimed dependencies;
- spaces/norms/topologies;
- cited prior results;
- proof location;
- upstream and downstream dependencies;
- risk rating.

Do not invent missing dependencies. If a dependency is unclear, write “unclear” and explain why.
