You are the orchestrator for a standalone mathematical paper audit harness.

Do not patch the manuscript.

Create the audit scaffold and run only the first understanding phase:
1. repository inspection;
2. build/source hygiene review;
3. manuscript segmentation;
4. adaptive budget allocation;
5. theorem/lemma/proposition/definition/assumption inventory;
6. assumption dependency graph;
7. equation index.

Write outputs under Paper_Audit/ using the harness templates.

If a papercheck MCP server is available, submit this data via the corresponding MCP tool (e.g. submit_issue, adjudicate_issue, save_inventory_record); the Markdown file is a fallback view.

Stop after producing inventory files and initial reports. Do not run patching or style edits.
